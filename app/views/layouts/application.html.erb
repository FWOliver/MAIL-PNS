<!DOCTYPE html>
<html>
<head>
  <title>Mailit</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
</head>

	
  <body>
		<div id="user_nav">
		  <% if model_signed_in? %>
		    Signed in as <%= current_model.email %>. Not you?
		    <%= link_to "Sign out", destroy_model_session_path, :method => :delete %>
		  <% else %>
		    <%= link_to "Sign up", new_model_registration_path %> or <%= link_to "sign in", new_model_session_path %>
		  <% end %>
			<%= yield %>
		</div>
  </body>

</html>
